# Voting poll

# 1. User Data
Usesrs date are being simply stored in local store as i havent got to the point of creating backend to store users data or relying on cloud backend. It would have taked me significantly wayy longer time to create an backend and test on it, to ensure that everything works, same with using cloud backend as example (Supabse or Firebase).

It works very simply, You'll able to create account as normal role or as administrator. To log in as Admin your password has to be "admin" otherwise all password that you'r gonna use will only give you an normal role.

# 2. As an admin you can createt polls + delete polls.

There's not really special case for an admin othe than creating polls and being able to delete them.
